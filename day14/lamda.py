# for to use the multiple function multiple times , we can use lamda function
# it is a anonamous funciton , bec it doesn't have name of the function 
a = lambda x : x*x
b = lambda y : y-4
print(a(20) , b(20))

c = lambda x,y,z : x+y*z
print(c(1,2,3))